<% if Current.user == product.seller %>
  <div class="dropdown">
    <button class="btn" type="button" data-bs-toggle="dropdown" aria-expanded="false">
      <%= icon "more-horizontal", class: "text-secondary-subtle", width: "32px", height: "32px" %>
    </button>
    <ul class="dropdown-menu dropdown-menu-end">
      <li>
        <%= link_to "Edit Product", edit_product_path(product), class: "dropdown-item" %>
      </li>
    </ul>
  </div>
<% end %>
